export default function Viewer() {
  return (
    <section className="viewer">
      <h1>Viewer</h1>
      <p>
        3D IFC viewer and void datagrid coming soon. This page will stack the 3D view on top and the
        void grid below, with project navigation, status editing, and selection sync between the
        grid and the 3D model.
      </p>
    </section>
  );
}
